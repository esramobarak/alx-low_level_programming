#include "main.h"
#include <stdio.h>
/**
 * _strlen - return the length of a string
 * @s : Parameter accept  string
 * Return: length of string
 *
 */

int _strlen(char *s)
{
int counter;

for (counter = 0; *s != '\0'; b++)
	++counter

return (counter);
}
