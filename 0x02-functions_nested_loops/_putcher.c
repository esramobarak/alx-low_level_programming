#include <unistd.h>
#include "main.h"
/**
 * putchar -writes the character c to stdout.
 * &c : the character to print.
 *
 * Return:on success 1
 *          on error -1 it returned
 */
int _putcher(char c)
	return(write (1 , &n , 1));
